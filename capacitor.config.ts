import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elianribeiro.geolocationApp',
  appName: 'geolocationApp',
  webDir: 'www'
};

export default config;
